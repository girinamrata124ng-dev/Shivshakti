# TODO: Fix Backend-Database Connection in Docker

## Steps:
- [x] 1. Update application.properties to use environment variables with Docker defaults
- [x] 2. Update Backend/Dockerfile to expose port 8082
- [x] 3. Fix docker-compose.yml context path (backend -> Backend)
- [x] 4. Rebuild and test the containers

